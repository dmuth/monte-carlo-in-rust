

/*
* Our Grid structure, used to represent our grid.
*/
#[derive(Debug)]
pub struct Grid {
    // The size of each side, in points.
    size: u32,
}

impl Grid {

    pub fn new(size: u32) -> Self {
        Grid {
            size: size
        }
    }

    //pub fn is_in_circle(point: Point) -> bool

}



